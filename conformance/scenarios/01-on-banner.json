{
  "id": "01-on-banner",
  "title": "on → 팀 배너가 출력된다",
  "spec_refs": ["02 §11.5", "01 §2.2"],
  "tier_signal": "deterministic",
  "steps": [
    {
      "name": "팀 모드 켜기",
      "action": { "kind": "command", "argv": ["on"] },
      "expect": [
        { "kind": "exit_code", "value": 0 },
        { "kind": "stdout_contains", "value": "teammode" }
      ]
    },
    {
      "name": "배너 캐시가 표준 위치에 존재",
      "action": { "kind": "noop" },
      "expect": [
        { "kind": "file_exists", "path": "memory/banner.txt" }
      ]
    }
  ]
}
