[project]
name = "teammode"
version = "0.1.0"
description = "Turn your team mode on — cross-agent team memory + hook/adapter engine"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-q"
