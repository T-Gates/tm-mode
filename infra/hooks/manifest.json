[
  {
    "event": "SessionStart",
    "script": "session-start.py",
    "timeout": 30,
    "mode": "on",
    "enforcement": "advisory",
    "_timeout_unit": "seconds",
    "_timeout_note": "네트워크 훅(do_reconcile: fetch+ff/rebase) — NET_TIMEOUT(10s) 순차 호출 최악치 + 여유. 3s 면 훅 러너가 git_ops 반환 전에 죽여 sync-warning 기록까지 날린다(codex P1)."
  },
  {
    "event": "UserPromptSubmit",
    "script": "session-log-remind.py",
    "timeout": 2,
    "mode": "on",
    "enforcement": "advisory",
    "_timeout_unit": "seconds"
  },
  {
    "event": "PostToolUse",
    "match": { "action": "file_edit" },
    "script": "auto-commit.py",
    "timeout": 40,
    "fallback": "runtime",
    "enforcement": "block",
    "_timeout_unit": "seconds",
    "_timeout_note": "#19 foreground push 복구: PUSH_TOTAL_BUDGET(22s) + 첫 로컬 index.lock 실패 경로(최대 8s) + 재시도 대기 1s + abort/ledger 정리 여유를 포함해 40s. runner kill 전에 pending/sync-warning fallback 을 반드시 기록한다."
  },
  {
    "event": "PreToolUse",
    "match": { "mcp": { "server": "linear", "tool": "create_issue" } },
    "script": "confirm-action.py",
    "args": "teammode-linear-create-allow",
    "timeout": 2,
    "fallback": "runtime",
    "enforcement": "block",
    "strict": true,
    "_timeout_unit": "seconds"
  },
  {
    "event": "PreToolUse",
    "match": { "action": "file_edit" },
    "script": "kb-write-guard.py",
    "timeout": 2,
    "fallback": "runtime",
    "enforcement": "block",
    "strict": true,
    "mode": "on",
    "_timeout_unit": "seconds",
    "_note": "KB 쓰기 거버넌스: memory/ 직접 편집 차단. memory 동사 경유만 허용."
  }
]
